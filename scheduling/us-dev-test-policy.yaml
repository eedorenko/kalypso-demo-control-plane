apiVersion: scheduler.kalypso.io/v1alpha1
kind: SchedulingPolicy
metadata:
  name: us-dev-test-policy
spec:
  deploymentTargetSelector:
    labelSelector:
      matchLabels:
        purpose: dev-test
  clusterTypeSelector:
    labelSelector: 
      matchLabels:
        country: us
