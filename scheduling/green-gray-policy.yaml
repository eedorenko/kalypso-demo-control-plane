apiVersion: scheduler.kalypso.io/v1alpha1
kind: SchedulingPolicy
metadata:
  name: green-gray-policy
spec:
  deploymentTargetSelector:
    labelSelector:
      matchLabels:
        zone: green
  clusterTypeSelector:
    labelSelector: 
      matchLabels:
        zone: gray
