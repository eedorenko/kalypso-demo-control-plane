apiVersion: v1
kind: ConfigMap
metadata:
  name: dev-config
  labels:
     platform-config: "true"
data:
  ENVIRONMENT: Dev1
  SOME_COMMON_ENVIRONMENT_VARIABLE: "true"
  CLUSTER_NAME: Factory Cluster
